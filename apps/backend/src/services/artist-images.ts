/**
 * @file Re-export from the consolidated image-cache module.
 *
 * Kept for backward compatibility so existing import paths
 * (`from "./artist-images.js"`) continue to work.
 */
export { cacheArtistImage, getArtistImages } from "./image-cache.js";
