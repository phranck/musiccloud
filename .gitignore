# Dependencies
node_modules/

# Brainstorming visual-companion scratch files (local-only)
.superpowers/

# Git worktrees (local isolated workspaces; reproducible via git worktree add)
.worktrees/

# Build output
dist/
build/
.build/
coverage/
*.lcov
*.tgz
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# graphify knowledge-graph output (generated, reproducible via /graphify)
graphify-out/

# gource dev-history render dir: large local input assets (background audio,
# avatars) and generated output (mp4/HLS), rendered via scripts/mkgource. The
# tracked config lives in .gource.conf (a file, not this dir).
.gource/

# Astro
.astro/

# Environment (all dotenv variants; keep a committable example if one is ever added)
.env
.env.*
!.env.example

# Secrets, keys & certificates (never publish)
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
*.p8
*.keystore
*.jks
*.asc
*.mobileprovision
.npmrc
.pgpass
id_rsa
id_ed25519
*.ppk
secrets.*
*.secret
credentials.json
service-account*.json
GoogleService-Info.plist

# Claude / agent / Serena / OpenSpec / notes (local only)
.continue/
.deepsec/
.kiro/
.pi/
.pi-lens/
.remember/
.serena/
.vibe/
openspec/
papers/
WHATS-NEXT.md

# .claude / .codex are tracked (plans, runbooks, skills, architecture, project
# knowledge), but local-only state and credentials must never be published:
.claude/settings.local.json
.claude/SESSION.md
.codex/SESSION.md
.claude/memory/*credential*

# Generated plan overview dashboard (reproducible via the `plans` tool)
/plans.html

# Xcode
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
*.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
DerivedData/
.swiftpm/
*.moved-aside
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

# LaTeX / Pandoc temp (docs PDF build)
*.aux
*.log
*.toc
*.out
*.fls
*.fdb_latexmk

# Hosting
.vercel/

# Local app runner
/.app/

# Local DB-dump helper (machine-specific: Zerops project id + local DB creds)
scripts/dbdump

# Personal / local-only reference docs (never publish — may contain private data)
/.private/

# OS
.DS_Store

# Xcode build number counter
apps/Apple/.build-number
