# Dependencies
node_modules/

# Brainstorming visual-companion scratch files (local-only)
.superpowers/

# Git worktrees (local isolated workspaces; reproducible via git worktree add)
.worktrees/

# Build output
dist/
build/
.build/
coverage/
*.lcov
*.tgz
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# graphify knowledge-graph output (generated, reproducible via /graphify)
graphify-out/

# Astro
.astro/

# Environment (all dotenv variants; keep a committable example if one is ever added)
.env
.env.*
!.env.example

# Secrets, keys & certificates (never publish)
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
*.p8
*.keystore
*.jks
*.asc
*.mobileprovision
.npmrc
.pgpass
id_rsa
id_ed25519
*.ppk
secrets.*
*.secret
credentials.json
service-account*.json
GoogleService-Info.plist

# Local agent instructions, plans, memories, and specifications
.agents/
.claude/
.codex/

# Gource sources, configuration, scripts, and generated renders (local only)
*gource*

# Other local-only agent / editor state and notes
.continue/
.deepsec/
.kiro/
.pi/
.pi-lens/
.remember/
.serena/
.vibe/
openspec/
papers/
WHATS-NEXT.md

# Local project references and visual design working files
/docs/
mockups/
architecture/

# Generated plan overview dashboard (reproducible via the `plans` tool)
/plans.html

# Xcode
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
*.xcodeproj/project.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
DerivedData/
.swiftpm/
*.moved-aside
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

# LaTeX / Pandoc temp (docs PDF build)
*.aux
*.log
*.toc
*.out
*.fls
*.fdb_latexmk

# Hosting
.vercel/

# Generated OpenAPI / Developer Portal build inputs
.tmp/openapi/
.tmp/sdk/
apps/developer/src/generated/

# Local app runner
/.app/

# Local DB-dump helper (machine-specific: Zerops project id + local DB creds)
scripts/dbdump

# Local Creem product seed helper (touches the Creem sandbox with a test key)
scripts/creem-seed.mjs

# Personal / local-only reference docs (never publish — may contain private data)
/.private/

# OS
.DS_Store

# Xcode build number counter
apps/Apple/.build-number
