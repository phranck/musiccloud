# Textsuche, Teil 2: Disambiguierungs-Liste und Folgeanfrage.

direction: down

input: "Aus erstem Durchgang\n(Vorderster unter Auto-Schwelle)" {
  shape: parallelogram
  style.fill: "#fff3cd"
}

disambiguation: "Disambiguierungs-Liste\n(nach Mindestkonfidenz gefiltert,\nin Größe begrenzt)" {
  shape: parallelogram
  style.fill: "#fff3cd"
}

input -> disambiguation

follow_up: "Authentifizierte Folgeanfrage\nmit ausgewähltem Kandidaten" {
  shape: hexagon
  style.fill: "#cce5ff"
}

disambiguation -> follow_up: "Client wählt aus,\nschickt zweite Anfrage" {
  style.stroke-dash: 4
}

selected: "Ausgewählten Kandidaten auflösen" {
  shape: step
}

follow_up -> selected

cache_isrc: "Cache-Suche per ISRC" {
  shape: cylinder
  style.fill: "#fff8e1"
}

cross: "Dienstübergreifende Suche" {
  shape: cloud
  style.fill: "#cce5ff"
}

resolved: "Aufgelöster Track" {
  shape: parallelogram
  style.fill: "#cce5ff"
}

selected -> cache_isrc: "Nach-Abruf-ISRC-Deduplikation"
cache_isrc -> cross: "Miss"
cross -> resolved
cache_isrc -> resolved: "Treffer"
